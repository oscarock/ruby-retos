# definir dos módulos de Ruby llamados System y Sales que contengan, 
# cada uno, una clase llamada Record.
# El evaluador intentará crear una instancia de cada una de 
# estas clases de la siguiente forma:

# system_record = System::Record.new
# sales_record = Sales::Record.new

module System
	class Record
	end
end

module Sales
	class Record
	end
end